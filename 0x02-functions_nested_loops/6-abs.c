#include "main.h"

/**
 * _abs - Print absolute value of an integer
 * @int: Represent integers
 *
 * Return: Success (0)
 */
int _abs(int a)
{	
	int a;
	if (a < 0)
	{
		a = (a * (-1));
		return (0);
	}
	else
	{
		a = a;
		return (0);
	}
}
