#include "main.h"

/**
 * _abs - Print absolute value of an integer
 * @int: Represent integers
 *
 * Return: Success (0)
 */
int _abs(int a)
{	
	int result;
	if (a < 0)
	{
		result = (a * (-1));
		return (result);
	}
	else
	{
		a = result;
		return (result);
	}
}
