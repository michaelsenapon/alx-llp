#include "main.h"

/**
 * _abs - Print absolute value of an integer
 * @int: Represent integers
 *
 * Return: Success (0)
 */
int _abs(int n)
{	
	int n;
	if (n < 0)
	{
		n = (n * (-1));
		return (0);
	}
	else
	{
		n = n;
		return (0);
	}
}
