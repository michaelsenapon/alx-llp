#include "main.h"
#include <stdio.h>

/**
 * print_to_98 - Print numbers from n to 98
 * @n: n represent the beginning of prints
 *
 * Return: Always 0
 */
void print_to_98(int n)
{
	if (n <= 98)
		for (i = n; i <= 98; i++)
			printf("%d, ", i);
			return;
	else
		for (i = n; i >=98; i==)
			printf("%d, ", i);
			return;
}
