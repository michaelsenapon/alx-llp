#include "main.h"

/**
 * mul - Function multiply 2 integers
 * @a: is first integer
 * @b: is second integer
 *
 * Return: Always (0) Success
 */
int mul(int a, int b);
{
	int result;

	result = (a * b);
	return (result);
}
